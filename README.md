# Blog Post Generator API

🚀 FastAPI приложение для автоматической генерации блог-постов с использованием актуальных новостей через Currents API и OpenAI GPT-4o-mini.

## 📋 Содержание

- [Возможности](#возможности)
- [Технологии](#технологии)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [API Документация](#api-документация)
- [Примеры использования](#примеры-использования)
- [Архитектура](#архитектура)
- [Обработка ошибок](#обработка-ошибок)
- [Безопасность](#безопасность)
- [Устранение неполадок](#устранение-неполадок)

## ✨ Возможности

- 🤖 **Автоматическая генерация постов** - создание полноценных блог-постов по заданной теме
- 📰 **Интеграция с новостями** - использование актуальных новостей через Currents API как контекст
- 📝 **SEO-оптимизация** - автоматическая генерация заголовков и мета-описаний
- 🔒 **Безопасность** - хранение API ключей через переменные окружения
- 📊 **Мониторинг** - эндпоинты для проверки работоспособности
- 🛡️ **Обработка ошибок** - полная обработка исключений с логированием
- 📚 **Автодокументация** - встроенная Swagger/ReDoc документация

## 🛠 Технологии

- **FastAPI** - современный веб-фреймворк для создания API
- **OpenAI GPT-4o-mini** - модель для генерации контента
- **Currents API** - сервис для получения актуальных новостей
- **Uvicorn** - ASGI сервер для запуска приложения
- **Pydantic** - валидация данных и модели
- **httpx** - асинхронный HTTP клиент
- **python-dotenv** - работа с переменными окружения

## 📦 Установка

### Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий или скопируйте файлы проекта**

2. **Создайте виртуальное окружение (рекомендуется):**
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate3. **Установите зависимости:**
pip install -r requirements.txt## ⚙️ Настройка

### 1. Получение API ключей

#### OpenAI API ключ

1. Зарегистрируйтесь на [OpenAI Platform](https://platform.openai.com/)
2. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый API ключ
4. Скопируйте ключ (он начинается с `sk-proj-...`)
5. ⚠️ **Важно**: Пополните баланс аккаунта для использования API

#### Currents API ключ

1. Зарегистрируйтесь на [Currents API](https://currentsapi.services/)
2. Перейдите в раздел API Keys
3. Скопируйте ваш API ключ
4. ✅ Бесплатный тариф включает 100 запросов в день

### 2. Настройка переменных окружения

1. **Скопируйте шаблон:**
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env2. **Откройте файл `.env` и заполните ключи:**
OPENAI_API_KEY=sk-proj-ваш-ключ-здесь
CURRENTS_API_KEY=ваш-ключ-здесь3. **⚠️ Важно**: Убедитесь, что файл `.env` добавлен в `.gitignore`:
```
.env
*.env
```

## 🚀 Запуск

### Режим разработки (с автоперезагрузкой)

```bash
python app.py
```

Или напрямую через uvicorn:
```bash
uvicorn app:app --reload --host 0.0.0.0 --port 8000
```

### Продакшн режим

```bash
uvicorn app:app --host 0.0.0.0 --port 8000 --workers 4
```

### С дополнительными опциями

```bash
uvicorn app:app \
  --host 0.0.0.0 \
  --port 8000 \
  --workers 4 \
  --log-level info \
  --access-log
```

После запуска приложение будет доступно по адресам:
- 🌐 **API**: http://localhost:8000
- 📖 **Swagger UI**: http://localhost:8000/docs
- 📚 **ReDoc**: http://localhost:8000/redoc

## 📖 API Документация

### Базовые эндпоинты

#### `GET /`
Корневой эндпоинт с информацией о API.

**Ответ:**
```json
{
  "message": "Blog Post Generator API",
  "version": "1.0.0",
  "docs": "/docs",
  "health": "/health"
}
```

#### `GET /health`
Проверка работоспособности сервиса и конфигурации API ключей.

**Ответ:**
```json
{
  "status": "healthy",
  "openai_configured": true,
  "currents_configured": true
}
```

**Статус коды:**
- `200` - Сервис работает нормально

### Основные эндпоинты

#### `POST /generate`
Генерация блог-поста по заданной теме.

**Тело запроса:**
```json
{
  "topic": "Преимущества медитации",
  "include_news": true,
  "max_news": 5,
  "language": "ru",
  "max_tokens": 2048
}
```

**Параметры:**

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `topic` | string | ✅ Да | - | Тема для генерации поста (мин. 1 символ) |
| `include_news` | boolean | ❌ Нет | `true` | Использовать ли актуальные новости |
| `max_news` | integer | ❌ Нет | `5` | Максимум новостей (1-20) |
| `language` | string | ❌ Нет | `"ru"` | Язык новостей (ru, en, etc.) |
| `max_tokens` | integer | ❌ Нет | `2048` | Максимум токенов (50-4096) |

**Успешный ответ (200):**
```json
{
  "title": "Медитация: Путь к внутреннему спокойствию и продуктивности",
  "meta_description": "Откройте для себя преимущества медитации и как она может улучшить вашу жизнь...",
  "post_content": "Медитация становится все более популярной практикой...",
  "news_used": [
    {
      "title": "Новое исследование о медитации",
      "description": "Ученые обнаружили...",
      "url": "https://example.com/news",
      "published": "2024-01-01T00:00:00Z"
    }
  ]
}
```

**Ошибки:**

| Код | Описание |
|-----|----------|
| `400` | Некорректные параметры запроса |
| `502` | Ошибка при обращении к внешним API |
| `503` | Сервис не настроен (отсутствуют API ключи) |
| `500` | Внутренняя ошибка сервера |

#### `GET /topics/suggestions`
Получить список примеров тем для генерации постов.

**Ответ:**
```json
{
  "topics": [
    "Преимущества медитации",
    "Здоровое питание для занятых людей",
    "Советы по управлению временем",
    ...
  ]
}
```

## 💡 Примеры использования

### Пример 1: Генерация поста с новостями (cURL)

```bash
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "topic": "Преимущества медитации",
    "include_news": true,
    "max_news": 5,
    "language": "ru"
  }'
```

### Пример 2: Генерация поста без новостей (cURL)

```bash
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "topic": "Здоровое питание",
    "include_news": false,
    "max_tokens": 1024
  }'
```

### Пример 3: Использование через Python

```python
import requests

# Генерация поста
response = requests.post(
    "http://localhost:8000/generate",
    json={
        "topic": "Цифровая трансформация бизнеса",
        "include_news": True,
        "max_news": 3,
        "language": "ru"
    }
)

if response.status_code == 200:
    post = response.json()
    print(f"Заголовок: {post['title']}")
    print(f"\nМета-описание: {post['meta_description']}")
    print(f"\nКонтент:\n{post['post_content']}")
    
    if post['news_used']:
        print(f"\nИспользовано новостей: {len(post['news_used'])}")
else:
    print(f"Ошибка: {response.status_code}")
    print(response.json())
```

### Пример 4: Асинхронный запрос (Python)

```python
import httpx
import asyncio

async def generate_post_async():
    async with httpx.AsyncClient() as client:
        response = await client.post(
            "http://localhost:8000/generate",
            json={
                "topic": "Экологичный образ жизни",
                "include_news": True
            }
        )
        return response.json()

# Запуск
result = asyncio.run(generate_post_async())
print(result)
```

### Пример 5: Использование через Swagger UI

1. Откройте http://localhost:8000/docs в браузере
2. Найдите эндпоинт `POST /generate`
3. Нажмите "Try it out"
4. Заполните параметры в JSON формате
5. Нажмите "Execute"

## 🏗 Архитектура

### Структура проекта

```
project/
├── app.py                 # Основное FastAPI приложение
├── requirements.txt        # Зависимости проекта
├── .env.example           # Шаблон переменных окружения
├── .env                   # Файл с API ключами (не в git)
└── README.md              # Документация
```

### Процесс генерации поста

1. **Получение запроса** - API получает тему и параметры
2. **Получение новостей** (опционально) - запрос к Currents API
3. **Форматирование контекста** - преобразование новостей в промпт
4. **Генерация заголовка** - создание привлекательного заголовка через OpenAI
5. **Генерация мета-описания** - создание SEO-оптимизированного описания
6. **Генерация контента** - создание полного текста поста с использованием новостей
7. **Возврат результата** - отправка сгенерированного поста клиенту

### Компоненты системы

- **FastAPI Application** - основной веб-сервер
- **OpenAI Client** - клиент для работы с OpenAI API
- **Currents API Client** - асинхронный клиент для получения новостей
- **Pydantic Models** - модели для валидации данных
- **Error Handlers** - обработчики ошибок и исключений
- **Logger** - система логирования

## ⚠️ Обработка ошибок

Приложение включает обработку следующих типов ошибок:

### HTTP Ошибки

- **400 Bad Request** - некорректные параметры запроса
- **502 Bad Gateway** - ошибки при обращении к внешним API (OpenAI, Currents)
- **503 Service Unavailable** - сервис не настроен (отсутствуют API ключи)
- **500 Internal Server Error** - внутренние ошибки сервера

### Типы исключений

- `openai.APIError` - ошибки OpenAI API
- `httpx.TimeoutException` - таймауты при запросах
- `httpx.HTTPStatusError` - HTTP ошибки от внешних сервисов
- Общие исключения - логируются для отладки

### Логирование

Все ошибки логируются с подробной информацией:
- Время возникновения
- Тип ошибки
- Детали запроса
- Stack trace (для критических ошибок)

## 🔒 Безопасность

### Рекомендации

1. **API ключи**
   - ✅ Храните ключи в `.env` файле
   - ✅ Добавьте `.env` в `.gitignore`
   - ✅ Никогда не коммитьте ключи в репозиторий
   - ✅ Используйте разные ключи для разработки и продакшна

2. **Продакшн окружение**
   - Используйте переменные окружения сервера вместо `.env`
   - Настройте HTTPS для защиты данных
   - Добавьте аутентификацию для API (если необходимо)
   - Ограничьте rate limiting для предотвращения злоупотреблений

3. **Мониторинг**
   - Настройте логирование в файлы
   - Используйте системы мониторинга (Prometheus, Grafana)
   - Отслеживайте использование API ключей

### Пример .gitignore

```
.env
*.env
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
.venv
```

## 🔧 Устранение неполадок

### Проблема: "OpenAI API не настроен"

**Решение:**
1. Проверьте наличие файла `.env`
2. Убедитесь, что `OPENAI_API_KEY` заполнен
3. Проверьте формат ключа (должен начинаться с `sk-proj-`)
4. Перезапустите приложение

### Проблема: "Currents API не работает"

**Решение:**
1. Проверьте наличие `CURRENTS_API_KEY` в `.env`
2. Проверьте лимиты бесплатного тарифа (100 запросов/день)
3. Убедитесь, что интернет-соединение работает
4. Проверьте логи на наличие ошибок

### Проблема: "Ошибка при генерации поста"

**Решение:**
1. Проверьте баланс OpenAI аккаунта
2. Убедитесь, что модель `gpt-4o-mini` доступна
3. Проверьте параметр `max_tokens` (не превышает лимиты)
4. Посмотрите логи приложения для деталей

### Проблема: "Порт уже занят"

**Решение:**
```bash
# Используйте другой порт
uvicorn app:app --port 8001

# Или найдите процесс, использующий порт
# Windows
netstat -ano | findstr :8000

# Linux/Mac
lsof -i :8000
```

### Проблема: "Модуль не найден"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
# Переустановите зависимости
pip install -r requirements.txt --upgrade
```

## 📊 Мониторинг и логи

### Просмотр логов

Логи выводятся в консоль в формате:
```
2024-01-01 12:00:00 - INFO - Получение новостей для темы: Медитация
2024-01-01 12:00:01 - INFO - Получено 5 новостей по теме 'Медитация'
2024-01-01 12:00:02 - INFO - Генерация заголовка...
```

### Уровни логирования

- **INFO** - информационные сообщения
- **WARNING** - предупреждения (например, отсутствие API ключей)
- **ERROR** - ошибки при выполнении операций
- **CRITICAL** - критические ошибки

## 🤝 Вклад в проект

Если вы хотите улучшить проект:

1. Создайте форк репозитория
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📝 Лицензия

Этот проект предоставляется "как есть" для образовательных целей.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Устранение неполадок"
2. Изучите логи приложения
3. Проверьте документацию API на `/docs`

## 🎯 Планы на будущее

- [ ] Кэширование результатов генерации
- [ ] Поддержка нескольких языков для генерации
- [ ] Интеграция с другими новостными API
- [ ] Экспорт постов в различные форматы (Markdown, HTML)
- [ ] Batch генерация постов
- [ ] Метрики и аналитика использования API

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024
```

Документация включает:
- Описание возможностей
- Инструкции по установке и настройке
- Полную документацию API
- Примеры использования
- Описание архитектуры
- Руководство по безопасности
- Раздел устранения неполадок

Скопируйте содержимое в файл `README.md` в корне проекта.
